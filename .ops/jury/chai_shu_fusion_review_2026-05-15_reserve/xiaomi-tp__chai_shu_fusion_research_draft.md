| severity | field_path | issue | suggestion |
| :--- | :--- | :--- | :--- |
| **高** | `conclusion定位模糊` | 结论将拆书定位为“参考作品分析层”，但未明确该层与 Ginga **现有 Foundation 层**（资产库）的**交互协议**和**边界**。是独立的“资产采集与预处理阶段”，还是 Foundation 下的一个“只读数据源”？模糊的定位可能导致后续实现时职责重叠或架构腐化。 | 1. 明确定义 `BookAnalysisLayer` 为 Ginga 的 **可选资产采集与预处理子系统**，其唯一输出是**去污染的、结构化的参考模式资产**，供 Foundation 消费。<br>2. 在架构图中明确标出其与 Foundation、RAG sidecar、workflow engine 的交互点及数据流向（见下条建议）。 |
| **高** | `architecture数据流` | 草案推荐了新 workflow `book_analysis_pipeline`，但未画出其与 **Ginga 核心架构组件（如 `CapabilityRegistry`, `StateIO`, `Foundation`）** 的具体交互序列。特别是：`BK_pattern_report` 等步骤产出的“泛化 pattern”如何被 `StateIO` 接收？如何进入 Foundation？该路径的安全审计点在哪里？ | 1. 增补一个**序列图**，展示 `book_analysis_pipeline` 的步骤如何调用新 provider，provider 如何返回结果，`StateIO.write_artifact()` 如何写入安全目录（如 `.ops/book_analysis/`），以及人工确认后资产如何通过新接口 promote 到 `Foundation`。<br>2. 显式定义一个新的 `StateIO.write_analysis_artifact()` 方法或利用现有 `write_artifact()` 并指定 `category='analysis'`，以隔离存储和审计。 |
| **高** | `priority.P0风险` | 将“**文风指纹 provider**”列为 P0 **可能过度激进**。文风指纹（stylometrics）计算依赖 `jieba`、`numpy`，且需要处理**空章节、中文引号、异常输入**，健壮性改造工作量不小。这与当前主线“P2-7C provider 质量验证”可能**争抢研发资源**，且其价值依赖于下游“风格锁定”功能，属于间接价值。 | 1. 将“文风指纹 provider”降级为 **P1**，P0 聚焦于更基础的