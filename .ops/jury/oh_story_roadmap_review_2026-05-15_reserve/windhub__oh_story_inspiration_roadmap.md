severity | field_path | issue | suggestion
---|---|---|---
高 | 三、未来路线建议 / v1.3：拆书融梗支线升级为 Evidence Pipeline | 章节原子事件提取（chapter_atom）和剧情聚合功能，虽然计划分步实现（P1/P2），但其输出（trope_recipe_candidate）和“孤立事件兜底”逻辑，存在将外部参考内容（拆书结果）无意中引入创作流程的风险。这可能导致参考内容污染创作核心状态或RAG。 | 明确禁止 `trope_recipe_candidate` 在任何阶段自动进入创作工作流。所有从参考书提取的“候选配方”必须经过显式的人工审核和批准步骤（例如，通过一个专门的 `promote` CLI命令），才能被后续创作流程使用。在validator中增加对输出路径的严格检查，确保无文件被写入 `foundation/runtime_state/` 或默认RAG索引目录。
高 | 三、未来路线建议 / v1.5：Story Review 与 Anti-AI Style Gate | “平台 rubric：番茄、起点、晋江、知乎盐言等作为 methodology asset” 的表述，虽然说明“不直接进入 prompt”，但作为“methodology asset”加载，仍可能通过skill或provider间接影响prompt构建，与“guard/checker 结果只做审计，不注入 prompt”的当前边界存在潜在冲突。 | 将“平台rubric”明确归类为仅供“review report”生成使用的只读分析数据。确保在架构上，这些rubric数据仅能被 `review` 相关的report生成函数读取，并完全隔离于所有用于构建创作请求（向LLM）的prompt组装路径。在代码审查中需重点检查相关数据流。
中 | 三、未来路线建议 / v1.4：Book Workspace View 与只读 Explorer | 引入 `BookView` 导出和 `ginga query` / read-only explorer 功能，虽然强调是“projection”和“不做创作建议”，但若实现不当（如缓存或衍生状态管理），可能在实际使用中被误解或依赖为“第二套状态真值”，与“StateIO 是 foundation/runtime_state 的唯一写入口”原则产生认知摩擦。 | 在 `BookView` 和 `explorer` 的所有输出报告和CLI帮助信息中，显著标注“此视图为派生数据，真值以 `foundation/runtime_state/` 为准”。确保所有查询功能均为无状态、实时从StateIO和artifacts计算得出，不建立任何长期存储的索引或缓存（或如有缓存，需明确标注为易失效的）。
中 | 三、未来路线建议 / v1.6：Market / Research Sidecar | 市场扫榜功能高度依赖外部平台数据采集，涉及“浏览器/平台采集”和“登录态”。这引入了显著的外部依赖波动性、法律合规风险及维护负担。在当前主线（P2-7 Platform runner收敛）阶段，此功能可能分散精力，ROI相对较低。 | 支持将此项延后至P2。在v1.6设计阶段，必须将数据采集模块设计为完全独立的、可选的插件或sidecar脚本，与核心的 `workflow DSL + skill adapters` 解耦。核心系统仅定义清晰的报告输入接口，而不包含任何采集实现。
低 | 四、优先级排序 / 表格 | 表格中将“v1.3 Evidence Pipeline 文档补强 + P0 validator”列为P0，与正文中“P0 只做 `scan / split / manifest / validator / report`”对应。但“P1 才做 `chapter_atom` 与质量门”的表述可能造成优先级混淆，因为“质量门”是防止污染的关键组件。 | 建议在 `ROADMAP.md` 中进一步澄清：v1.3 的 P0 阶段严格限定于基础设施（扫描、切分、清单、验证框架），产出仅为原始章节块和清单报告。所有涉及内容分析（原子提取、聚合、质量门）的功能，即使借鉴自信，也必须明确置于 P1 或之后，并附带更严格的污染控制验收标准。
低 | 二、Ginga 吸收原则 / 段落 | 原则中“不把参考原文进默认 RAG”和“拆书结果必须保留在 sidecar”的表述清晰，但在后续路线（如v1.3）中，仅靠输出路径约束可能不足，缺乏对“默认RAG”索引构建过程的主动防护机制描述。 | 建议在架构设计或验收标准中，明确增加一条防护规则：任何写入 `.ops/book_analysis/<run_id>/` 目录的、包含外部参考内容提取物的文件，其路径必须被加入一个“RAG排除列表”（例如 `.ragignore`），由核心的RAG索引构建流程主动读取并忽略，形成双保险。
低 | 五、不建议吸收的部分 / 段落 | 指出“不建议把原文拆书库作为默认 RAG source”，并给出了“只允许 sidecar、hash、污染检查、人工 promote”的正面指引，这对于控制污染风险是明确的。但未在后续路线建议的验收标准中反复强调此点。 | 在 v1.3, v1.6 等涉及外部数据处理的路线验收标准中，应重复并具体化此条原则。例如，在v1.3的validator中，明确检查是否生成了包含“禁止自动索引”标记的元数据文件，并由CI流程验证该标记的有效性。