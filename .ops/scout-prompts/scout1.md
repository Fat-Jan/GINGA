你是 ginga 小说系统蒸馏项目的 Scout 1，深扫 `/Users/arm/Desktop/ginga/_原料/基座/`（544 个 md 文件）。

## 项目背景（必读）

主 agent 正在把 1000+ 个小说创作提示词模板蒸馏成一个分层系统底座（Meta → Foundation → Platform → RAG 四层）。你是 4 个并行 Scout 中的第 1 个，负责"基座"目录。用户要求"不要省事简单方案，多思考多花时间"。

基座目录结构（已确认）：
- `基座/题材/网文/` — 题材模板（替身文/规则怪谈/多子多福/黑暗多子多福/狗血女文/知乎短篇/情满四合院/欲念描写专家 等）
- `基座/模板/{伏笔,大纲,悬念,角色,项目,世界观}/` — 通用模板
- `基座/方法论/{写作,创意,市场,平台}/` — 方法论

文件命名规范：`编号-题材-阶段-用途.md`（如 `380-欲念描写专家-微观-欲念心理.md`）。
文件内容结构：`# 标题` → `## 系统角色` → `## 思维链指令`（分层）→ `## 输出要求`。

## 你的任务

深度扫描 + 字段抽取，**不要省事**：

1. **抽样阅读**：每个子目录至少完整读 8 个代表性文件（题材/网文 抽不同题材各 3-5 个，模板 6 个子分类各 2-3 个，方法论 4 个子分类各 2 个）。绝不只读文件名。

2. **统一 frontmatter schema 设计**：基于实际样本，给出推荐的 YAML frontmatter 字段表，每个字段标注：类型、是否必填、取值范围、来源。

3. **三维标签矩阵**：题材维度 / 阶段维度（设定/框架/创作/数据分析/进阶/微观/中观/宏观/势力/辅助/商业化/...） / 用途维度。给出实际分布表。

4. **思维链层级模式分析**：基座模板的"## 思维链指令"通常分几层？每层主题模式？

5. **模板间依赖发现**：哪些模板会调用/依赖其他模板？给出至少 5 个具体依赖示例。

6. **Top 高价值模板挑选**：选出 15-20 个结构最完整、复用度最高的模板（写明编号 + 入选理由）。

7. **跨题材方法论复用度分析**：方法论目录的模板有多少可以跨题材复用？

## 输出要求

**把详细报告 Write 到** `/Users/arm/Desktop/ginga/.ops/scout-reports/scout1-base.md`，必须包含：

- `## Schema 草案`（YAML frontmatter 字段表）
- `## 三维标签矩阵`（题材 × 阶段 × 用途）
- `## 思维链层级模式`
- `## 模板依赖关系示例`
- `## Top 高价值模板`
- `## 方法论跨题材复用度`
- `## 关键发现与建议`

工作目录 `/Users/arm/Desktop/ginga`。绝不省事。直接动手读文件、抽样、写报告。完成后只用一段话告知报告路径 + 实际读了多少文件即可。
